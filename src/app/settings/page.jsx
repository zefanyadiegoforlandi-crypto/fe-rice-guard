import SettingsClient from './SettingsClient';

export const metadata = {
  title: 'Setting — SekarPadi',
  description: 'Pengaturan akun SekarPadi',
};

export default function SettingsPage() {
  return <SettingsClient />;
}
